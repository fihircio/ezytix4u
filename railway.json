{
  "$schema": "https://railway.app/v2.schema.json",
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate --force && apache2-foreground",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
