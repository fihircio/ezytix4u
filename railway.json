{
  "$schema": "https://railway.app/v2.schema.json",
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate:fresh --force && apache2-foreground",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
